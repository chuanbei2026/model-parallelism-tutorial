# Sequence Parallelism (SP)

Sequence parallelism: splitting the sequence dimension across GPUs to reduce activation memory. Covers Megatron-style SP (coupled with TP), LayerNorm/Dropout partitioning, and communication trade-offs.
