# Data Parallelism (DP)

Data parallelism: replicating the model across GPUs and splitting data batches. Covers vanilla DP, DistributedDataParallel (DDP), gradient synchronization via AllReduce, ZeRO optimization stages, and FSDP.
